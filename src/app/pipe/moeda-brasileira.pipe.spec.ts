import { MoedaBrasileiraPipe } from './moeda-brasileira.pipe';

describe('MoedaBrasileiraPipe', () => {
  it('create an instance', () => {
    const pipe = new MoedaBrasileiraPipe();
    expect(pipe).toBeTruthy();
  });
});
